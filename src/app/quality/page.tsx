import { ComingSoon } from "@/components/ComingSoon";

export default function QualityPage() {
  return (
    <ComingSoon
      title="Quality & CRM audit"
      subtitle="Automated data-quality monitor across Close CRM, Airtable, Intercom and VendHub."
      description="Will surface duplicates, missing fields, and verification drift so ops can self-heal the pipeline every morning."
    />
  );
}
